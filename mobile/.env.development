# Local development API — used automatically by `expo start` / debug builds
# (NODE_ENV=development). Points at this computer's LAN IP so a phone on
# the same Wi-Fi can reach the locally-running Flask dev server directly.
# Re-check with `ipconfig` and update this value if it changes.
EXPO_PUBLIC_API_URL=http://192.168.1.120:5000/api
