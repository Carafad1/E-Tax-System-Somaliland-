# Production API — used automatically by release/production builds
# (NODE_ENV=production, set by the RN/Expo build tooling). This is the
# permanent cloud backend; it never depends on any developer's computer.
EXPO_PUBLIC_API_URL=https://etax-api-56s7.onrender.com/api
